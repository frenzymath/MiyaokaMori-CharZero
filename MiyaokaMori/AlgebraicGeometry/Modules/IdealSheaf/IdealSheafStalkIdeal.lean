import MiyaokaMori.Prelude

/-! # The stalk ideal of a quasi-coherent ideal sheaf

The stalk ideal `I_x ⊆ O_{X,x}` of a quasi-coherent ideal sheaf `I` (Mathlib's `IdealSheafData`)
at a point `x`: the ideal generated by the images under the germ maps of `I(U)`, over all affine
opens `U` containing `x`.

References: Stacks 01QZ (quasi-coherent ideal sheaves); the statements of Stacks 0AHH / 0AGT
use `I_x`.
-/

set_option autoImplicit false
set_option maxHeartbeats 400000

universe u v w u' v'

open CategoryTheory CategoryTheory.Limits Opposite TopologicalSpace
open scoped AlgebraicGeometry

noncomputable section

/-- The stalk ideal `I_x := ⨆_{U ∋ x affine} I(U)·O_{X,x}`. It does not depend on the choice
of `U` (`stalkIdeal_eq_map_germ`). -/
def AlgebraicGeometry.Scheme.IdealSheafData.stalkIdeal {X : AlgebraicGeometry.Scheme.{u}}
    (I : X.IdealSheafData) (x : X) : Ideal (X.presheaf.stalk x) :=
  ⨆ (U : X.affineOpens) (hx : x ∈ U.1), (I.ideal U).map (X.presheaf.germ U.1 x hx).hom

end
