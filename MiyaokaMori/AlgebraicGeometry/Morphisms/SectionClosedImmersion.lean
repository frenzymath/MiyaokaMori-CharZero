import MiyaokaMori.Prelude
import MiyaokaMori.AlgebraicGeometry.Morphisms.SectionOfSeparatedIsClosedImmersion

/-! # Sections of separated morphisms are closed immersions

A section of a separated morphism is a closed immersion. This is what makes the complement of the
`L`-section (or of the zero section) in `P(O ⊕ L)` an open subscheme (§2.1 of the paper).
-/

set_option autoImplicit false
set_option maxHeartbeats 400000

universe u v w u' v'

open CategoryTheory CategoryTheory.Limits Opposite TopologicalSpace
open scoped AlgebraicGeometry

noncomputable section



end
